{{#if comics }}
    <section class="comics">
        <h1>{{ comicsPageTitle }}</h1>
        {{#each comics as |comic|}}
            {{#> comic}}
                <article>
                    <img src="{{ comic.thumbnail }}" alt="{{comic.title}} Cover Image">
                    <a href="/comics/{{comic.id }}">
                        <h2>{{comic.title}}</h2>
                    </a>
                </article>
            {{/comic}}
        {{/each}}
    </section>
    <a href="/" class="btn more">more comics</a>
{{/if}}