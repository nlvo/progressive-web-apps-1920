{{#if character}}
    <section class="character">
        <header>
            <img src="{{ character.thumbnail }}"  alt="Image from the {{ character.title }} character.">
            <div>
                <h1>{{ character.title }}</h1>
            </div>
        </header>
    </section>
{{/if}}