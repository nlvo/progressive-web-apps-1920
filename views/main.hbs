{{> head }}
<main>
    {{> header }}
    <div class="container">
        {{> nav }}
        <section>
            {{> comics }}                
            {{> characters }}
            {{> comic-detail }}
            {{> search }}
        </section>
    </div>
</main>
{{> footer}}
