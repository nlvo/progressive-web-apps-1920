{{> head }}
<main>
    {{> header }}
    <div class="container">
        {{> nav }}
        <section>
            {{> comics }}                
            {{> characters }}
            {{> comic-detail }}
        </section>
    </div>
</main>
{{> footer}}
